<div class="container-fluid">
  <div class="row">
    <header>
      <div class="col-md-12">
        <img src="assets/images/mark.png" alt="Mike Yockey logo" class="img-responsive center-block logo">
      </div>
    </header>
  </div>

  <div class="row">
    <div class="col-md-offset-5 col-md-2">
      <nav>
        <ul class="nav nav-pills nav-justified">
          {{#link-to 'home' tagName="li"}}
            {{link-to 'Home' 'home'}}
          {{/link-to}}
          {{#link-to 'about' tagName="li"}}
            {{link-to 'About' 'about'}}
          {{/link-to}}
        </ul>
      </nav>
    </div>
  </div>

  <div class="col-md-offset-1 col-md-10">
    {{outlet}}
  </div>
</div>
